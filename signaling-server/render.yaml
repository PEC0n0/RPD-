# Render Blueprint：一键部署 SyncWatch 信令服务器（免费 Web Service）
# 在 Render 控制台 New > Blueprint 选择本仓库即可自动创建服务。
services:
  - type: web
    name: syncwatch-signaling
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
